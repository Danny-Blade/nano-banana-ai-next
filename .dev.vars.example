# Local-only environment variables for `wrangler dev`.
# Copy to `.dev.vars` (it is git-ignored) and fill in your real values.
#
# Run:
#   yarn build:local
#   yarn dev:worker

# NextAuth (wrangler dev 和 yarn dev 都使用 3000 端口)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=REPLACE_WITH_YOUR_NEXTAUTH_SECRET

# Google OAuth (optional if using DEV_AUTH_BYPASS)
GOOGLE_CLIENT_ID=REPLACE_WITH_YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=REPLACE_WITH_YOUR_GOOGLE_CLIENT_SECRET

# Upstream image generation (choose one)
APIYI_API_KEY=REPLACE_WITH_YOUR_APIYI_API_KEY
# NANO_BANANA_API_KEY=REPLACE_WITH_YOUR_APIYI_API_KEY

# Dev-only auth bypass (recommended for `wrangler dev` if Google OAuth callback fails)
DEV_AUTH_BYPASS=false
DEV_AUTH_EMAIL=rping1230@gmail.com
DEV_AUTH_CREDITS=1000
